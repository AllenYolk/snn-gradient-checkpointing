seed_everything: 2024

trainer:
  deterministic: True
  max_epochs: 192
  precision: 32
  num_sanity_val_steps: 0
  default_root_dir: ./ # training logs will be saved at `./lightning_logs`

model:
  neuron_type: SJLIF
  compress_x: True
  level: 0
  T: 16


data:
  data_dir: /export/home/data_allenyolk/DVSGesture
  T: 16
  batch_size: 16
  num_workers: 4
